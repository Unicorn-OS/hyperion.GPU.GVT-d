#source: https://github.com/intel/gvt-linux/wiki/GVTg_Setup_Guide

# install
sudo apt-get install git libfdt-dev libpixman-1-dev libssl-dev vim socat libsdl1.2-dev libspice-server-dev autoconf libtool xtightvncviewer tightvncserver x11vnc libsdl1.2-dev uuid-runtime uuid uml-utilities bridge-utils liblzma-dev libc6-dev libdrm-dev libgbm-dev libaio-dev libusb-1.0.0-dev bison


# Uninstallable
sudo apt-get install \
libegl1-mesa-dev \
python-dev \
libepoxy-dev \
libgtk-3-dev

## Can be installed one at a time separately! But for some reason won't install as a list?

# Repair Desktop
## Installing all this may break Ubuntu! reinstall:
sudo apt install ubuntu-desktop -y


# unlisted deps
sudo apt install flex \
libelf-dev \
-y
